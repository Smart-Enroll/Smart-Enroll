<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard</title>

  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag 'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>

  <style>
  body {
    font-family: Arial, sans-serif;
    background-color: #f4f4f4;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    height: 100vh;
  }

  .top-banner, .bottom-banner {
    background-color: #006747;
    width: 100%;
    height: 100px; 
  }

  .dashboard-container {
    text-align: center;
    flex-grow: 1;  
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
  }

  .welcome-text h2 {
    color: #006747; 
    font-size: 24px;
    font-weight: bold;
    margin-top: 20px;
    margin-left: 20px;
    align-self: flex-start;
  }

  .button-container {
    display: flex;
    justify-content: center;
    gap: 20px; 
    margin-top: 40px;
  }

  .green-button {
    background-color: #006747;
    color: white;
    border: none;
    padding: 20px;
    font-size: 18px;
    cursor: pointer;
    border-radius: 5px;
    width: 200px; 
  }

  .green-button:hover {
    background-color: #004d00;
  }
</style>
</head>
<body>

  <!-- Top Banner with title -->
  <div class="top-banner">
    <h2 style="text-align: center; color: white; line-height: 100px; font-size: 24px;">
      Welcome to Your Dashboard
    </h2>
  </div>

  <div class="dashboard-container">
    <!-- Welcome text in green, same as banners -->
    <div class="welcome-text">
      <h2>Welcome, <%= @student.name %>!</h2>
    </div>

    <!-- Buttons inside a form, placed horizontally -->
    <div class="button-container">
      <button class="green-button" onclick="window.location.href='<%= schedule_view_index_path %>';">Go to Schedule</button>
      <button class="green-button">Other Action 1</button>
      <button class="green-button">Other Action 2</button>
    </div>
  </div>

  <!-- Bottom Banner -->
  <div class="bottom-banner"></div>
  <%= button_to "Logout", logout_path, method: :delete, class: "logout-button" %>

</body>
</html>

