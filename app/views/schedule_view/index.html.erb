<!DOCTYPE html>
<html>
<head>
  <title>Student Course Schedule</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag 'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>

  <style>
  body {
  background-color: #004B2D; /* USF Green */
  color: white;
  font-family: Arial, sans-serif;
  margin: 0;
  padding: 0;

  
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
}

.container {
  background: white;
  color: black;
  border-radius: 10px;
  width: 100vw;
  padding: 20px;
  max-width: 1200px;
  margin: 40px auto;
  text-align: center;
  height: 70vh;

 
  display: flex;
  flex-direction: column;
  justify-content: space-between; 
}

.header {
  background-color: #004B2D;
  color: white;
  padding: 15px;
  border-radius: 10px 10px 0 0;
  display: flex;
  align-items: center;
  justify-content: space-between;
  position: relative;
}

.header img {
  width: 100px; 
  position: relative;
  z-index: 2;
}

.course-container {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 10px;
  margin-top: 20px;
  flex-grow: 1; 
}

.course-card {
  background: lightgray;
  border-radius: 10px;
  padding: 15px;
  text-align: center;
  font-weight: bold;
  position: relative;
}

.status {
  display: block;
  padding: 5px;
  font-size: 14px;
  border-radius: 5px;
  position: absolute;
  bottom: 5px;
  left: 50%;
  transform: translateX(-50%);
}

.status.ready {
  background: green;
  color: white;
}

.status.full {
  background: yellow;
  color: black;
}

.status.conflict {
  background: red;
  color: white;
}

.info-icon {
  position: absolute;
  top: 5px;
  right: 10px;
  font-size: 18px;
  cursor: pointer;
}

.add-button {
  background: lightgray;
  border-radius: 10px;
  padding: 30px;
  font-size: 24px;
  text-align: center;
  cursor: pointer;

 
  display: flex;
  align-items: center;
  justify-content: center;
}

.footer {
  margin-top: 20px;
  font-weight: bold;
}

.back-button {
  position: absolute;
  bottom: 20px;
  left: 20px;
  background: #004B2D;
  color: white;
  padding: 15px 20px; 
  font-size: 20px;
  border-radius: 5px;
  border: none;
  cursor: pointer;
}

.back-button:hover {
  background: #003F26;

.help-button {
  position: absolute;
  bottom: 20px;
  right: 20px;
  background: #004B2D;
  color: white;
  padding: 10px 15px;
  border-radius: 5px;
  border: none;
  cursor: pointer;
}

.help-button:hover {
  background: #003F26; 
}


  </style>
</head>
<body>

  <div class="container style="width: 100p">
    <div class="header">
      <img src="<%= asset_path('LogoWithCircle.png') %>" alt="USF Logo">
      <h2>Student Course Schedule for <span style="color: yellow;"><%= @term %></span></h2>
      <i class="settings-icon">⚙️</i>
    </div>

    <div class="course-container">
      <% @user_schedules.each do |schedule| %>
        <div class="course-card">
          <i class="info-icon">ℹ️</i>
          <span class="status <%= schedule.status.downcase.gsub(' ', '-') %>">
            <%= schedule.status %>
          </span>
        </div>
      <% end %>
      <div class="add-button">+</div>
    </div>

    <button class="back-button">← Back</button>

    <div class="footer">
      <span>Help ?</span>
    </div>
  </div>

</body>
</html>
