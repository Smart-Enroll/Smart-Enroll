<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            height: 100vh;
        }

        .top-banner, .bottom-banner {
            background-color: #006747;
            width: 100%;
            height: 50px;  /* Adjust the height as needed */
        }

        .login-container {
            background: white;
            padding: 40px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            width: 600px;
            text-align: center;
            border: 5px solid #006747;
            margin: 20px auto; /* Centers the login container */
        }

        .logo {
            width: 100px; /* Adjust size as needed */
            height: auto;
            display: block;
            margin: 0 auto 20px; /* Centers and adds space below */
        }

        .header {
            background: #006747;
            color: white;
            padding: 20px;
            font-size: 24px;
            font-weight: bold;
            border-radius: 5px 5px 0 0;
        }

        .input-container {
            margin: 15px 0;
            text-align: center;
        }

        .input-container label {
            font-size: 18px;
            font-weight: bold;
            display: block;
            margin-bottom: 5px;
        }

        .input-container input {
            width: 50%;
            padding: 12px;
            border: 2px solid #006747;
            border-radius: 5px;
            font-size: 16px;
        }

        .forgot-link {
            color: purple;
            font-size: 14px;
            display: block;
            text-decoration: none;
            margin-top: 5px;
        }

        .login-button {
            width: 40%;
            padding: 10px;
            background: #006747;
            color: white;
            border: none;
            border-radius: 5px;
            font-size: 18px;
            cursor: pointer;
            margin-top: 20px;
        }

        .login-button:hover {
            background: #004d00;
        }

        .password-container {
            position: relative;
        }

        .password-container input {
            width: 50%;
        }

        .toggle-password {
            position: absolute;
            right: 110px;
            top: 50%;
            transform: translateY(-50%);
            cursor: pointer;
        }
    </style>
</head>
<body>
    <div class="top-banner"></div>  <!-- Top Banner -->
  
    <div class="login-container">
        <!-- Logo added here -->
        <%= image_tag 'circular_logo.png', alt: 'Smart Enroll Logo', class: 'logo' %>
        <div class="header">WELCOME TO SMART ENROLL</div>
        <form action="login.php" method="POST">
            <div class="input-container">
                <label for="email">Email:</label>
                <input type="email" id="email" name="email" placeholder="Enter Your Email" required>
                <a href="#" class="forgot-link">Forgot Email</a>
            </div>
            <div class="input-container password-container">
                <label for="password">Password:</label>
                <input type="password" id="password" name="password" placeholder="Enter Your Password" required>
                <span class="toggle-password">👁</span>
                <a href="#" class="forgot-link">Forgot Password</a>
            </div>
            <button type="submit" class="login-button">Login</button>
        </form>
    </div>

    <div class="bottom-banner"></div>  <!-- Bottom Banner -->
</body>
</html>